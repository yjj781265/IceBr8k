<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="HardcodedText"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:layout_marginTop="2dp"
    android:animateLayoutChanges="true"
    android:elevation="2dp">


    <ImageView
        android:id="@+id/outcoming_image_avatar"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        app:layout_constraintBottom_toTopOf="@+id/outcoming_image_time"
        app:layout_constraintStart_toStartOf="parent"
        app:srcCompat="@drawable/camera_selector"/>


    <ImageView
        android:id="@+id/incoming_image_imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:adjustViewBounds="true"
        android:minWidth="100dp"
        android:minHeight="100dp"
        android:contentDescription="image"
        android:maxHeight="150dp"
        android:maxWidth="150dp"
        android:transitionName="photoView"
        app:layout_constraintBottom_toTopOf="@+id/outcoming_image_time"
        app:layout_constraintStart_toEndOf="@+id/outcoming_image_avatar"/>


    <TextView
        android:id="@+id/outcoming_image_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="123"
        android:textSize="12sp"
        android:visibility="visible"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="@+id/incoming_image_imageView"/>




</android.support.constraint.ConstraintLayout>